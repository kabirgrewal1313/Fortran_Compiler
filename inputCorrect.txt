PROGRAM demo ::

INTEGER :: i, j, sum
REAL :: x
LOGICAL :: flag

sum = 0
flag = .TRUE.

DO i = 1, 5

    j = i + 1
    sum = sum + i * j
    IF i < 3 THEN
        sum = sum + 10
    END

    flag = i < j .AND. flag

END

x = sum + 2.5

END